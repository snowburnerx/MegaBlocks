#!/usr/bin/python


class BuildComponentHotspot:

    def __init__(self, config):
        self.type = config['type']
        self.x = config['x']
        self.y = config['y']
        self.z = config['z']
